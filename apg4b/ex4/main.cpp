#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main()
{
	int seconds = 365 * 24 * 60 * 60;
	cout << seconds * 1 << endl;
	cout << seconds * 2 << endl;
	cout << seconds * 5 << endl;
	cout << seconds * 10 << endl;

	return 0;
}
