#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (n); ++i)
using P = pair<int, int>;

int main(void) {
  // 自分の得意な言語で
  // Let's チャレンジ！！
  int N, H, W, P, Q;
  cin >> N >> H >> W >> P >> Q;

  vector<P> an;
  for (int i = 0; i < N; ++i) {
    int a, b;
    cin >> a >> b;
    an.emplace_back(a, b);
  }
  
}